\documentclass[11pt]{article}
\usepackage[margin=2.2cm]{geometry}
\usepackage{amsmath,amssymb,booktabs,enumitem}
\usepackage[hidelinks]{hyperref}
\setlist{nosep,leftmargin=*}
\setlength{\parindent}{0pt}

\newcommand{\dt}{\tilde\delta}
\newcommand{\nb}{\bar n}
\newcommand{\R}{\mathbb R}

\title{01 --- Departure from CSR: $\dt$ and null tables}
\date{17 September 2026}

\begin{document}
\maketitle

%==============================================================================
\section{Theory}

\subsection*{Setting}
\begin{itemize}
  \item Window $W=[0,1]^2$, fixed
  \item $\nb$: expected number of points (deterministic model parameter)
  \item Pattern $x = \{x_1,\dots,x_n\} \subset W$; $n = |x|$ realised number of points (random around $\nb$)
  \begin{itemize}
    \item Poisson, Mat\'ern II: $n/\nb \in [0.90, 1.11]$ (5--95\%)
    \item Thomas, nested, LGCP: $n/\nb \in [0.77, 1.25]$ (overdispersed, clusters arrive whole)
  \end{itemize}
\end{itemize}

\subsection*{Estimator}
\begin{itemize}
  \item Ripley's $K$, isotropic edge correction, $\lambda^2$ estimated by $n(n-1)$:
  \[
    \hat K_x(r) = \frac{1}{n(n-1)} \sum_{i\neq j} w(x_i, d_{ij})\, \mathbf 1\{d_{ij}\le r\},
    \qquad
    w(x,d) = \frac{2\pi}{\text{length of } \partial b(x,d)\cap W}
  \]
  \item Centred $L$-function of a pattern $x$, and its model counterpart:
  \[
    \hat\phi_x(r) = \hat L_x(r) - r = \sqrt{\hat K_x(r)/\pi} - r,
    \qquad
    \phi(r) = L(r) - r \quad (= 0 \text{ under CSR})
  \]
  \item Evaluated on the radius grid: $\hat\phi_x = \big(\hat\phi_x(r_1),\dots,\hat\phi_x(r_{512})\big) \in \R^{512}$
  \item Radii $r_k = 0.25\,k/512$, $k=1,\dots,512$ ($r=0$ dropped: identically zero)
\end{itemize}

\subsection*{Radius set}
Everything in this subsection is defined for fixed $n$. The test and $\dt$ only use radii in $R(n)$.
\begin{itemize}
  \item At small $r$, $\hat\phi_x(r)$ is a function of a small count: the pairs of $x$ closer than $r$. Under $\mathbb P_{0,n}$ we expect
  \[
    \mu(r;n) = \tfrac12 n(n-1)\pi r^2
  \]
  such pairs (edge-corrected)
  \item The maximum in $M_n$ (next subsection) treats all radii alike only if each standardised deviation $(\hat\phi_X(r) - m_0)/s_0$ has roughly the same null distribution, i.e.\ $\approx N(0,1)$
  \item Few expected pairs $\Rightarrow$ $\hat\phi$ takes a handful of values; standardised deviation discrete and skewed, not $N(0,1)$. Checked on the null curves at $n=99$:
  \begin{itemize}
    \item $\mu < 0.3$: heavy right tail, $\mathbb P(|z|>3)$ 10$\times$ Gaussian
    \item $\mu \in [3, 10)$: still 3$\times$ Gaussian
    \item $\mu \ge 10$: Gaussian tail rate, skewness and kurtosis
  \end{itemize}
  \item Size does not need them to be Gaussian ($c_{95}$ is simulated). Two things do bind:
  \begin{itemize}
    \item Heavy-tailed radii win the null maximum too often $\Rightarrow$ $c_{95}$ up (2.75--3.21 at $p=10$, 2.87--3.28 at $p=5$) $\Rightarrow$ 1--3 points of power lost at every other radius
    \item $c_{95}(n)$ must stay smooth in $n$, or the spline across $n$ (Smoothing) cannot be fitted honestly
  \end{itemize}
  \item The smoother is what fails first. Below $p\approx5$ the raw $c_{95}$ develops structure at large $n$ that no spline follows (residual s.d.\ vs Monte Carlo noise; $z$ against the fit):

  \smallskip
  \begin{tabular}{@{}lrrr@{}}
  \toprule
  $p$ & $c_{95}$ mean $z^2$ (expect $\approx1$) & residual s.d. & Monte Carlo noise \\
  \midrule
  10 & 0.77 & 0.019 & 0.022 \\
  \textbf{5} & \textbf{0.97} & \textbf{0.023} & \textbf{0.022} \\
  4 & 1.62 & 0.036 & 0.029 \\
  3 & 5.22 & 0.040 & 0.026 \\
  \bottomrule
  \end{tabular}

  \smallskip
  At $p=3$ the excess is concentrated at large $n$ (residual s.d.\ 0.056 for $n\ge1000$) and locally correlated (acf 0.30): the max statistic's null distribution is lumpy there, so its 95\% quantile moves in jumps. Covariates for the grid geometry do not absorb it (mean $z^2$ 5.22 $\to$ 4.67 at best)
  \item Keep radii with at least $p$ expected pairs, $p = 5$:
  \[
    R(n) = \{r_k : \mu(r_k;n) \ge p\} = [r_{\min}(n), 0.25],
    \qquad
    r_{\min}(n) = \sqrt{\frac{2p}{\pi n(n-1)}} \approx \frac{1.78}{n}
  \]
  \item $p$ is chosen from null diagnostics alone (size, $c_{95}$ smoothness), never from any alternative's power
  \item $r_{\min}$: 0.092 ($n=20$), 0.018 ($n=100$), 0.0022 ($n=800$), 0.00089 ($n=2000$). More points $\Rightarrow$ $R(n)$ reaches down to finer scales
\end{itemize}

\subsection*{Null distribution}
Everything in this subsection is defined for fixed $n$. In practice, we compute the null distribution at every $n$ on a grid.
\begin{itemize}
  \item Null $\mathbb P_{0,n}$: $n$ i.i.d.\ uniform points on $W$ (CSR conditioned on $n$); $\mathbb E_{0,n}$, $\mathrm{sd}_{0,n}$ under it
  \item Generate $12\,000$ CSR clouds from $\mathbb P_{0,n}$
  \item Estimate null moments for $r \in R(n)$ using half of the CSR clouds:
  \[
    m_0(r;n) = \mathbb E_{0,n}\big[\hat\phi_X(r)\big],
    \qquad
    s_0(r;n) = \mathrm{sd}_{0,n}\big[\hat\phi_X(r)\big]
  \]
  \item Using remaining half of CSR clouds, compute maximum deviation of each cloud $x$:
  \[
    M_n(x) = \max_{r\in R(n)} \frac{|\hat\phi_x(r) - m_0(r;n)|}{s_0(r;n)}
  \]
  \item Determine critical value: $c_{95}(n)$ = 95\% quantile of $M_n$ over these clouds (estimates the 95\% quantile of $M_n(X)$, $X\sim\mathbb P_{0,n}$)
  \item Repeated on a grid of $n$; $m_0$, $s_0$, $c_{95}$ then smoothed across $n$ (cubic splines in $\log n$) $\Rightarrow$ less Monte Carlo noise, defined at every $n$
\end{itemize}

\subsection*{Studentised global envelope (Myllym\"aki et al.\ 2017)}
\[
  S(x) = \frac{M_n(x)}{c_{95}(n)}, \qquad n = |x|
\]
\begin{itemize}
  \item Under the null, $\mathbb P_{0,n}(S > 1) = 0.05$ by definition of $c_{95}$ (up to Monte Carlo error)
  \item Reject CSR at 5\% iff $S > 1$
\end{itemize}

\subsection*{Distance $\dt$}
\[
  \dt(\theta,\nb) = \frac{1}{c_{95}(\nb)} \max_{r\in R(\nb)} \frac{|L_\theta(r) - r|}{s_0(r;\nb)}
\]
\begin{itemize}
  \item Same functional as $S$, applied to the closed-form $L_\theta$ at $\nb$, uncentred ($m_0 = 0$)
  \item $\dt(\text{CSR}) = 0$ exactly
  \item $\dt = 1$: true curve on the rejection boundary $\Rightarrow$ power well above 5\%; measured 0.64--0.78 (02), not 50\%, because pattern noise adds to the maximum
  \item Units: null s.d.s, relative to the critical value
  \item Property of the model, known before simulation $\Rightarrow$ usable as a sweep coordinate
  \item At fixed shape, $\dt$ grows with $\nb$ ($s_0$ shrinks)
  \item Depends on the cutoff when the departure lives below $r_{\min}$: a hard core of radius $R<r_{\min}$ enters $\dt$ only through the pair deficit it leaves above $r_{\min}$, diluted by the CSR count there, so its $\dt$ scales with $p$ (Mat\'ern II; 02)
\end{itemize}

%==============================================================================
\section{Implemented pipeline}

\subsection*{Code}
\begin{tabular}{@{}ll@{}}
\toprule
\texttt{src/cloudforger/classical/lfunction.py} & $\hat K$, $\hat L - r$, weights, $L-r$ from $K - \pi r^2$ \\
\texttt{src/cloudforger/departure/config.py}   & config, $n$ grid, paths \\
\texttt{src/cloudforger/departure/simulate.py} & null patterns $\to$ stored curves \\
\texttt{src/cloudforger/departure/fit.py}      & curves $\to$ spline tables \\
\texttt{src/cloudforger/departure/tables.py}   & tables $\to$ $S$, $\dt$ \\
\texttt{src/cloudforger/departure/validate.py} & size on fresh patterns \\
\texttt{scripts/departure.py}                  & CLI: \texttt{simulate}, \texttt{fit}, \texttt{validate} \\
\texttt{configs/departure/}                    & \texttt{config.yaml}, \texttt{tables.npz}, \texttt{report.json} \\
\bottomrule
\end{tabular}

\subsection*{Configuration}
\begin{itemize}
  \item $n$ grid: $\mathrm{round}(20 \cdot 1.03^k)$ up to 2000 $\Rightarrow$ 153 values
  \item Per grid $n$: 6000 fit + 6000 calibration patterns
  \item Validation: 50\,000 patterns, $n$ log-uniform integer on $[20, 2000]$
  \item $p = 5$ pairs; $\alpha = 0.05$; knots: 8 interior (moments), 4 interior ($c_{95}$)
\end{itemize}

\subsection*{Steps}
\begin{enumerate}
  \item \textbf{Simulate}: null curves for every grid $n$, stored
  \item \textbf{Fit}: moments and $c_{95}$ per $n$, splines in $\log n$ $\to$ \texttt{tables.npz}
  \item \textbf{Validate}: size on fresh patterns at random $n$
  \item \textbf{Relabel} (\texttt{scripts/relabel.py}): after a refit, recompute $\dt$ for every swept $\theta$ under the new tables; manifests keep \texttt{delta} (the sampling target) and \texttt{delta\_tilde} (current, what regimes use)
\end{enumerate}

\subsection*{Results}
\begin{tabular}{@{}lll@{}}
\toprule
Check & Result & Reference \\
\midrule
Fitted $m_0$ vs raw, mean $z^2$ & 0.94 & $\approx 0.92$ ($1 - \text{params}/\text{grid}$) \\
Fitted $s_0$ vs raw, mean $z^2$ & 0.96 & $\approx 0.92$ \\
Max relative error of $s_0$ & 4.0\% & --- \\
Fitted $c_{95}$ vs raw, mean $z^2$ & 0.97 & $\approx 0.95$ \\
$c_{95}$ range; max s.e. & 2.87--3.28; 0.030 & --- \\
Size, 50\,000 patterns & 0.0518 ($z = 1.8$) & 0.05 \\
Size by $n$ band & 0.049--0.056, all $|z| \le 2.4$ & 0.05 \\
Size, 20\,000 Poisson sweep patterns & 0.0496 & 0.05 \\
Centring gap $\max|m_0|/s_0/c_{95}$ & 0.026--0.044 & --- \\
\bottomrule
\end{tabular}

\subsection*{Estimator checks}
\begin{itemize}
  \item \texttt{tests/test\_lfunction.py}:
  \begin{itemize}
    \item Weights vs brute-force circle fraction: $|\Delta| < 10^{-3}$
    \item Two points at distance 0.1: $\hat K = 0$ below, $=1$ above
    \item $\mathbb E\hat K = \pi r^2$ under binomial (400 patterns, within 3\%)
  \end{itemize}
  \item One-off: agrees with the previous implementation (\texttt{baselines/vihrs.py}) to $10^{-9}$
\end{itemize}

Further statistical notes: \texttt{01\_departure\_notes.md}.

\end{document}
