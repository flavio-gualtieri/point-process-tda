# src/cloudforger/evaluation/__init__.py
"""Regime-resolved evaluation on DV3 (see dv3.py for why it exists).

    dv3          sets, paths, regime coordinates, the pre-assigned split
    predictions  the per-pattern prediction bundle every method writes
    regimes      bundle + manifest -> per-regime tables (params/classify/detect)
    classical    the studentised envelope test on L, G, F as a CSR detector
"""
